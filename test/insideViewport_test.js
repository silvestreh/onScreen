// import { expect } from 'chai';
// import insideViewport from '../lib/insideViewport';

describe('Visibility checking', () => {
    it('should be able to detect when an element is inside the viewport', () => {
        // FIXME: Switch to phantomjs as jsdom can't test these
    });
});
